import { Item } from "./Item";

export type Mockup = {
  item: Item;
  primaryColor: string;
  secondaryColor: string;
  configColor: string;
  companyLogoUrl: string;
  logoOnBackground: boolean;
  companyName: string;
};
